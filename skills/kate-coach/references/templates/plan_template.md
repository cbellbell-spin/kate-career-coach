## Standing Goals

[Target roles, level, and timeline as established during onboarding or updated since. Kept short — this is a pointer back to user_profile.md, not a duplicate of it. Update only when the goal itself changes, not every session.]

## Pending Commitments

[Anything Kate has offered to do, or the user has asked Kate to do, that isn't finished yet. Each entry is added the moment the offer or ask is made — not after the fact, not only if it happens to land in a coaching note.

Format: `[Date raised] | [What] | [Source: "Kate offered" / "User asked"] | [Status: Not started / In progress / Done / Dropped — reason]`

Example: `2026-07-19 | Set up weekly monitoring on 6 target companies | Kate offered | Not started`

Remove an entry only when it's Done or explicitly Dropped — don't let it silently disappear. If dropped, keep a one-line reason.]

## Flagged Gaps

[Populated by the Capability Checklist Flow (`references/flows.md`), run at SessionStart per kate-coach SKILL.md Step 5D. Tracks which "what's set up vs. untapped" gaps have already been surfaced to the user once, so they aren't repeated every session.

Format: `[Date] | [Gap name] | Flagged`

An entry is removed when the gap resolves (e.g., the user adds source materials), so it can be re-flagged if the gap recurs later. Kate maintains this section automatically — the user never edits it directly.]

## Review Log

[One line per session where this file was reviewed at close, even if nothing changed. Format: `[Date] — [Reviewed, no changes / Updated: brief note]`. This exists so a stale plan.md is visibly stale, not silently stale.]
